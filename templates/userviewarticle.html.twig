{% extends "userview.html.twig" %}

{% block content2 %}
    {% if message %}   
        {{message}}
    {% else %}
        <div class="news-number">ID={{article.id}}</div>
        <div class="title">{{article.title}}</div>
        <div class="text">{{article.text}}</div>
    {% endif %}   
{% endblock %}




